<!DOCTYPE html>
<html>
<head>
    <title>Editor</title>
</head>
<body>
    <div class="w3-container">
    <h1>Lista de Preguntas</h1>
    <table class="w3-table-all">
        <thead>
            <tr class="w3-light-grey">
                <th>Pregunta</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        {{#questions}}
        <tr>
            <td>{{contenido}}</td>
            <td>{{#estado}}Activa{{/estado}}{{^estado}}Inactiva{{/estado}}</td>
            <td>
                <a href="/editor/toggleQuestionState/id={{id}}" class="w3-button w3-green">{{#estado}}Desactivar{{/estado}}{{^estado}}Activar{{/estado}}</a>
                <a href="/editor/manageQuestionView/id={{id}}" class="w3-button w3-blue">Editar</a>
                <a href="/editor/deleteQuestion/id={{id}}" class="w3-button w3-red" onclick="return confirm('¿Estás seguro de borrar esta pregunta?')">Borrar</a>
            </td>
        </tr>
        {{/questions}}
    </table>
    <div class="w3-margin-top"> 
    <a href="/editor/manageQuestionView" class="w3-button w3-green">Agregar Nueva Pregunta</a>
    </div>
    </div>
</body>
</html>
