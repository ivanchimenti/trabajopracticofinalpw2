<!DOCTYPE html>
<html>
<head>
    <title>Editor</title>
</head>
<body>
    <div class="questions-container">
        <h1 class="questions-title">Lista de Preguntas</h1>
        <table class="w3-table-all questions-table">
            <thead>
                <tr class="w3-light-grey questions-header">
                    <th class="question-col">Pregunta</th>
                    <th class="status-col">Estado</th>
                    <th class="actions-col">Acciones</th>
                </tr>
            </thead>
            {{#questions}}
            <tr class="question-row">
                <td class="question-content">{{contenido}}</td>
                <td class="question-status">{{#estado}}Activa{{/estado}}{{^estado}}Inactiva{{/estado}}</td>
                <td class="question-actions">
                    <a href="/editor/toggleQuestionState/id={{id}}" class="action-toggle w3-button w3-green">{{#estado}}Desactivar{{/estado}}{{^estado}}Activar{{/estado}}</a>
                    <a href="/editor/manageQuestionView/id={{id}}" class="action-edit w3-button w3-blue">Editar</a>
                    <a href="/editor/deleteQuestion/id={{id}}" class="action-delete w3-button w3-red" onclick="return confirm('¿Estás seguro de borrar esta pregunta?')">Borrar</a>
                </td>
            </tr>
            {{/questions}}
        </table>
        <div class="add-question-button w3-button w3-green">
            <a href="/editor/manageQuestionView" class="btn-green">Agregar Nueva Pregunta</a>
        </div>
    </div>
</body>
</html>
